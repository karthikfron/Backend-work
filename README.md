# 📚 Express.js CRUD with SQLite – Goodreads Clone (Practice Project)

This project is built to practice **Express.js CRUD operations** using **SQLite** as a database.

---

## 🧩 Features
- GET all books
- GET book by ID
- POST (Add) a new book
- PUT (Update) book details
- DELETE a book
- GET all books by an author

---

## 🧠 What I Learned
- Setting up an Express server  
- Using `express.json()` for parsing POST/PUT request bodies  
- Connecting Express with SQLite using `sqlite` and `sqlite3` packages  
- Using SQL queries (`SELECT`, `INSERT`, `UPDATE`, `DELETE`)  
- Writing REST APIs (`GET`, `POST`, `PUT`, `DELETE`)  
- Using Postman to test routes  
- Using Git commands:
  ```bash
  git add .
  git commit -m "commit message"
  git push origin main
